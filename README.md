<h2> FastAPI and Pytest example </h2>

for test app you need to download requirements.txt then where app.py is run server with command:

```
uvicorn app:app --reload
```

then you can test API with command:

```
pytest
```
